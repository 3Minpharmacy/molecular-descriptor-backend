# =============================================================================
# render.yaml — Render.com deployment configuration
# =============================================================================
# Deploy this repository at https://render.com by connecting your GitHub
# repository and selecting "Blueprint" in the dashboard.
#
# Documentation: https://render.com/docs/blueprint-spec
# =============================================================================

services:
  - type: web
    name: molecularmind-api
    runtime: python
    region: oregon                      # Closest to most users; change as needed
    plan: starter                       # Upgrade to standard for production workloads
    # Render clones your repo and runs commands from the repo root.
    # rootDir is set explicitly so the path is unambiguous.
    rootDir: .
    buildCommand: pip install -r requirements.txt
    # Single worker — scale via Render's instance count, not --workers.
    # PYTHONPATH=. ensures `from app.routes import ...` resolves from the repo root.
    startCommand: PYTHONPATH=. uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health

    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: PYTHONPATH
        value: "."
      - key: LOG_LEVEL
        value: INFO
      - key: RELOAD
        value: "false"
      - key: MAX_ATOM_COUNT
        value: "500"
      # Set ALLOWED_ORIGINS in the Render dashboard as a secret env var:
      # ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://yourdomain.com
      - key: ALLOWED_ORIGINS
        value: "*"                      # Restrict in production!
      - key: ENABLE_MORDRED
        value: "false"

    autoDeploy: true                    # Redeploy on every push to the main branch
