# =============================================================================
# MolecularMind — Production Dependencies
# =============================================================================
# Pin minor versions for reproducible builds; patch versions are allowed to
# float for security updates.  Use `pip-compile` (pip-tools) to generate
# a fully locked requirements.txt for deployment.
# =============================================================================

# --- Web framework & server --------------------------------------------------
fastapi>=0.111.0,<0.113.0
uvicorn[standard]>=0.29.0,<0.31.0       # Includes uvloop + httptools for production performance
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0        # BaseSettings for environment-variable config

# --- Chemistry ---------------------------------------------------------------
# RDKit is distributed via conda-forge or the PyPI wheel (rdkit package).
# The PyPI wheel (rdkit) covers CPython 3.9–3.12 on Linux / macOS / Windows.
rdkit>=2023.9.5

# --- Data science ------------------------------------------------------------
numpy>=1.26.0,<2.1.0
pandas>=2.2.0,<3.0.0

# --- Optional: Mordred descriptor expansion ----------------------------------
# Uncomment to enable extended descriptor computation (500+ descriptors).
# Requires: pip install mordred
# mordred>=1.2.0

# --- Optional: ML pipeline compatibility -------------------------------------
# scikit-learn>=1.4.0,<2.0.0

# --- HTTP / async utilities --------------------------------------------------
httpx>=0.27.0,<0.28.0                   # Async HTTP client (useful for service-to-service calls)
python-multipart>=0.0.9                 # Required by FastAPI for form data

# --- Observability -----------------------------------------------------------
python-json-logger>=2.0.7               # Structured JSON logging for log aggregation pipelines
